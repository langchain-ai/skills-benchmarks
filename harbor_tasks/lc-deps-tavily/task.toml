schema_version = "1.3"

[task]
name = "skillbench/lc-deps-tavily"
description = "Dependency confusion task: Claude fixes a broken project with wrong langchain deps and import paths"

[metadata]
source_task = "lc-deps-tavily"
difficulty = "medium"
category = "langchain"
tags = ["langchain", "dependencies", "tavily", "imports", "debugging"]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 120.0
user = "root"

[environment]
build_timeout_sec = 600.0
workdir = "/workspace"
# skills_dir = "skills"  # Phase 1: per-treatment skills copied to the agent skills config dir
