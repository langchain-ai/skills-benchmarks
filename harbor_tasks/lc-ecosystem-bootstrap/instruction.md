I'm starting a new agent project. It needs a custom workflow with branching logic — depending on intermediate results the agent takes different paths, and at one step it should pause for human approval before continuing. I'll be using Anthropic for the model.

Set up the project so I can start building. Produce two files:
- `install.sh` — installs every package I need
- `.env.example` — all environment variables I need to develop and run the agent

Don't write the agent itself yet. Only produce those two files.
