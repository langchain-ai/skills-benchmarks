schema_version = "1.3"

[task]
name = "skillbench/lc-framework-research-assistant"
description = "Framework selection: build a research assistant with delegating sub-agents — Claude must pick create_deep_agent"

[metadata]
source_task = "lc-framework-research-assistant"
difficulty = "medium"
category = "langchain"
tags = ["langchain", "langgraph", "deep-agents", "framework-selection", "subagents"]

[agent]
timeout_sec = 180.0

[verifier]
timeout_sec = 120.0
user = "root"

[environment]
build_timeout_sec = 600.0
workdir = "/workspace"
# skills_dir = "skills"  # Phase 1: per-treatment skills copied to the agent skills config dir
