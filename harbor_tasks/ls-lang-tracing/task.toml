schema_version = "1.3"

[task]
name = "skillbench/ls-lang-tracing"
description = "Add LangSmith tracing to Python and TypeScript agents"

[metadata]
source_task = "ls-lang-tracing"
difficulty = "easy"
category = "langsmith"
tags = ["tracing", "polyglot", "python", "typescript"]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 120.0
user = "root"

[verifier.env]
LANGSMITH_API_KEY = "${LANGSMITH_API_KEY}"

[environment]
build_timeout_sec = 600.0
workdir = "/workspace"
# skills_dir = "skills"  # Phase 1: per-treatment skills copied to the agent skills config dir

[environment.env]
LANGSMITH_API_KEY = "${LANGSMITH_API_KEY}"
