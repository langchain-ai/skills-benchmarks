Create a TypeScript file called `hello.ts` that exports a `greet` function.

The function should:
- Take a `name` parameter (string)
- Return a greeting string like "Hello, {name}!"
- Export the function as a named export

Example usage:
```typescript
import { greet } from "./hello.js";
console.log(greet("World")); // "Hello, World!"
```
