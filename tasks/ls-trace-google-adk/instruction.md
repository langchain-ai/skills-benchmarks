Add LangSmith tracing to the Google ADK agent at `/workspace/agent.py`.

After instrumenting, run the agent once so a trace is emitted to LangSmith. Save the most recent trace ID to `trace_id.txt` at the project root.
