Our support agent (traced to the LangSmith project in $LANGSMITH_PROJECT, root run name `support_agent`; its harness lives in `agent/`) is getting complaints.

Review its recent production sessions and produce `fixes.md`: a prioritized list of concrete harness fixes. For each fix: the problem pattern, the LangSmith run IDs evidencing it, the root cause in this repo (file/section), and the specific change.

Then apply every fix that targets the harness files in `agent/` directly (edit the files).
