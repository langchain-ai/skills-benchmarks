I'm starting a new agent project with LangChain that uses Anthropic models. Give me an env file with all the vars I'll need to develop.

The file will be reviewed, not loaded. Only produce `.env.example`.
