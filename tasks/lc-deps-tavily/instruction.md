I want to build a LangChain agent that uses Tavily for web search. Can you create one for me?

Save it as agent.py and run it with the test query: "What is LangChain?"

IMPORTANT: Run files directly (not in background). If code fails after 2 attempts to fix, save the file and report the error - do not enter debug loops.
