schema_version = "1.3"

[task]
name = "skillbench/oss-fix-lg-execution"
description = "Fix Send API fan-out, interrupt, and resume bugs in a LangGraph pipeline"

[metadata]
source_task = "oss-fix-lg-execution"
difficulty = "medium"
category = "langgraph"
tags = ["send", "fan-out", "interrupt", "reducer", "bug-fix"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0
user = "root"

[environment]
build_timeout_sec = 1800.0
workdir = "/workspace"
cpus = 4
memory_mb = 8192
storage_mb = 65536
# skills_dir = "skills"  # Phase 1: per-treatment skills copied to the agent skills config dir
