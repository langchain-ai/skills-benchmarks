# LangSmith treatments
#
# The three LangSmith skills together, with and without distractor noise.
# (The old CLAUDE.md-placement, section-curation, and py/ts-split variants are
# archived under treatments/_archive/ — those dimensions no longer exist:
# language is a run-time --language flag, and there is no CLAUDE.md injection.)

LS_BASE:
  description: "LangSmith trace + evaluator + dataset skills"
  skills:
    - benchmarks/langsmith_trace
    - benchmarks/langsmith_evaluator
    - benchmarks/langsmith_dataset

LS_NOISE:
  description: "LangSmith skills + distractor noise skills"
  skills:
    - benchmarks/langsmith_trace
    - benchmarks/langsmith_evaluator
    - benchmarks/langsmith_dataset
    - noise/api_docs
    - noise/react_components
