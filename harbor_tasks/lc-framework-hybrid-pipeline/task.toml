schema_version = "1.3"

[task]
name = "skillbench/lc-framework-hybrid-pipeline"
description = "Framework selection: build a Deep Agents orchestrator + LangGraph sub-workflow — Claude must use create_deep_agent + CompiledSubAgent"

[metadata]
source_task = "lc-framework-hybrid-pipeline"
difficulty = "hard"
category = "langchain"
tags = ["langchain", "langgraph", "deep-agents", "framework-selection", "compiled-subagent"]

[agent]
timeout_sec = 180.0

[verifier]
timeout_sec = 120.0
user = "root"

[environment]
build_timeout_sec = 600.0
workdir = "/workspace"
# skills_dir = "skills"  # Phase 1: per-treatment skills copied to the agent skills config dir
