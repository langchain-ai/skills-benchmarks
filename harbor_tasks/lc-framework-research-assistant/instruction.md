Build a complete, working component and save it to `agent.py`. Choose the right framework based on its requirements.

An open-ended agent that breaks a research question into sub-tasks and delegates to specialized sub-agents.

Do not execute the file — the validator will statically check it for correct framework usage and complete imports.
