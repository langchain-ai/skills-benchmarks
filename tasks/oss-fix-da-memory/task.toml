[metadata]
name = "oss-fix-da-memory"
description = "Fix memory routing and subagent configuration bugs"
difficulty = "medium"
category = "deepagents"
tags = ["memory", "subagents", "persistence", "bug-fix"]

[template]
required = []

[environment]
timeout_sec = 600

[validation]
# Pattern-based validation checking for fixes from da_memory + da_subagents
validators = ["MemoryPatternValidator", "SubagentPatternValidator"]
