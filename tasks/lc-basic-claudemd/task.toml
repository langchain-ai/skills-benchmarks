[metadata]
name = "lc-basic-claudemd"
description = "Tests whether CLAUDE.md is needed and what instructions work best"
difficulty = "medium"
category = "langchain"
tags = ["langchain", "agents", "claude-md"]
default_treatments = ["CONTROL", "ALL_MAIN_SKILLS", "MAIN_SINGLE_PRIMER", "MAIN_SPLIT_PRIMER_CLAUDE"]

[template]
required = []

[environment]
dockerfile = "environment/Dockerfile"
timeout_sec = 900

[validation]
validators = ["validators"]
